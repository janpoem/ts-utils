[**@zenstone/ts-utils**](../README.md)

***

[@zenstone/ts-utils](../globals.md) / isString

# Function: isString()

> **isString**(`val`): `val is string`

Defined in: index.ts:290

检查值是否为字符串

## Parameters

### val

`unknown`

任意值

## Returns

`val is string`

## Example

```ts
if (isStr(value)) {
  console.log(value.toUpperCase());
}
```
